{% extends 'base.html.twig' %}

{% block body_id %}

{% endblock %}

{% block main %}

    <div style="text-align: center;">
        <span style="font-size: 50px;">
            {{ user.username }}
            <br/>
        </span>
        <span style="text-align: center;font-size: 20px">
            <br/>
            Full name: {{ user.fullName }}
            <br/>
            Email: {{ user.email }}
            <br/>
            <br/>
            <a href="{{ path('view_user_uploads', {'id': user.id}) }}">View user songs</a>
            <br/>
            <a href="{{ path('view_user_favorites', {'id': user.id}) }}">View user favorite songs</a>
        </span>
    </div>
{% endblock %}

