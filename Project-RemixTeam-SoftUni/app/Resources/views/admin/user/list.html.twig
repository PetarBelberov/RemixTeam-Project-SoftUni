{% extends 'base.html.twig' %}

{% block main %}
    <div class="container body-content" id="listUsers">
        <div class="well" id="wellAdmin">
            <h2 class="listTitle">All Users</h2>
            <div class="row">
                <table class="table table-striped table-hover ">
                    <thead>
                    <tr>
                        <th>#Id</th>
                        <th>Username</th>
                        <th>Full Name</th>
                        <th>Email</th>
                        <th>Action</th>

                    </tr>
                    </thead>
                    <tbody>
                    {% for user in users %}

                        <tr>
                        <tr {% if user.isAdmin() %}class="info" {% endif %}>
                            <td>{{ user.id }}</td>
                            <td><a id="deleteUsername" href="{{ path('view_user_uploads', {'id': user.id}) }}">{{ user.username }}</a></td>
                            <td>{{ user.fullName }}</td>
                            <td>{{ user.email }}</td>
                            <td>
                                <a href="{{ path('admin_user_delete', {'id':user.id}) }}">Delete</a>
                            </td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>
    </div>
{% endblock %}
